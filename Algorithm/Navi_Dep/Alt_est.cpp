#include "Alt_est.h"

// float bfcs_Acc[Axis_Sum];   /* accelerate on body fixed coordinate system */
// float gcs_Acc[Axis_Sum];    /* accelerate on geodetic coordinate system */


float BaroAltEstimate_Update(float baro, Matrix<float, 3, 1> acc)
{
    float tmp = 0.0f;

    return tmp;
}

